<h1>Task Manager</h1>
<%= link_to 'Create New Task', new_task_path %>

<% @tasks.each do |task| %>
  <h2><%= link_to task.title, task_path(task) %></h2>
  <p><%= task.details %></p>
  <%= link_to "🗑",
            task_path(task),
            method: :delete,
            data: { confirm: "Are you really quite sure?"} %>
  <% if task.completed == false %>
    <%= link_to task_path(task) do %>
      <%= fa_icon "square" %>
    <% end %>
  <% else %>
    <%= fa_icon "check-square" %>
  <% end %>
<% end %>

